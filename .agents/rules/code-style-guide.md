---
trigger: always_on
---

1. use shadcn as always if you can 
2. use tanstack query for every fetching mechanism - then wrapped into custom hook in ./hooks folder
3. make very clear style or standard shadcn - no dark mode - this repo is for internal tool, so clarity in fonts, style etc matter more than being beautiful 